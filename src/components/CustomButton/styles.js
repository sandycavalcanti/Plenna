import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  button: {
    width: '55%',
    backgroundColor: '#595D7C',
    borderRadius: 30,
    padding: 10,
    alignItems: 'center',
  },

  buttonText: {
    color: '#fff',
    fontSize: 16,
  },
});